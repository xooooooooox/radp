stages:
  - debug

debug:
  stage: debug
  tags:
    - k8s-linux
  only:
    - feat/241228-0.6
  script:
    - whoami
    - cat /etc/os-release
    - uname -a
    - echo "--------------------------"
    - echo ">>>${X9X_HTTP_PROXY}<<<"
    - echo "--------------------------"
    - echo "GitLab CI/CD | Print all environment variables"
    - env