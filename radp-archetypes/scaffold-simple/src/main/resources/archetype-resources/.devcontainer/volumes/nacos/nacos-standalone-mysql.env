# Nacos 环境变量配置（用于 .devcontainer/templates/nacos/docker-compose.yml）
# 参考: https://github.com/nacos-group/nacos-docker
# 说明:
# - 本文件示例以单机模式 + 外部 MySQL 为默认配置。
# - 请先在 MySQL 中创建数据库和账户，并与下方配置保持一致。
# - 生产环境请务必修改密码、令牌等敏感信息。

# 注册服务时优先使用主机名（容器名），方便容器内服务间通信。
PREFER_HOST_MODE=hostname

# 启动模式: standalone(单机) 或 cluster(集群)。此模板默认单机。
MODE=standalone

# 使用的持久化类型。mysql 表示将配置数据存储到外部 MySQL。
SPRING_DATASOURCE_PLATFORM=mysql

# MySQL 主机（容器名或IP）。若使用模板中的 mysql 服务，保持为 mysql。
MYSQL_SERVICE_HOST=mysql

# Nacos 使用的数据库名。请确保该库已存在。
MYSQL_SERVICE_DB_NAME=nacos_devtest

# MySQL 端口，默认 3306。
MYSQL_SERVICE_PORT=3306

# 连接 MySQL 的用户名。
MYSQL_SERVICE_USER=nacos

# 连接 MySQL 的密码（请在生产环境中修改）。
MYSQL_SERVICE_PASSWORD=nacos

# 连接参数：字符集/超时/SSL/时区/公钥检索等设置。
# 可按需调整，例如: useSSL=false 在内网环境常用；serverTimezone 需与实际时区一致。
MYSQL_SERVICE_DB_PARAM=characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true

# 鉴权相关：用于标识网关/客户端的身份键和值。
# 注意：如果启用鉴权（NACOS_AUTH_ENABLE=true, default is false），需为生产环境自定义并妥善保密。
NACOS_AUTH_IDENTITY_KEY=2222
NACOS_AUTH_IDENTITY_VALUE=2xxx

# 鉴权令牌（Base64 编码）。请在生产环境更换为强随机值：
# 生成示例：echo -n 'YourStrongSecret' | base64
NACOS_AUTH_TOKEN=VGhpc0lzTXlDdXN0b21TZWNyZXRLZXkwMTIzNDU2Nzg=
