###############################################################################
# 0) Ignore all project files first
###############################################################################
**

###############################################################################
# 1) Whitelist
###############################################################################
# root aggregator POM
!pom.xml
# sub-module pom
!**/pom.xml

# sub-modules
!${appName}-api/
!${appName}-app/
!${appName}-domain/
!${appName}-infrastructure/
!${appName}-trigger/
!${appName}-types/

# 构建脚本 / 配置
!mvnw*
!.mvn/
!.springjavaformatconfig
!.coding

###############################################################################
# 2) 二次排除 —— 避免白名单额外引入
###############################################################################
# ── Build / IDE / VCS ─────────────────────────────────────────────────────
**/target/
**/out/
**/build/
**/.m2/
**/.gradle/
**/.git/
**/.gitmodules
**/.idea/
**/.vscode/
*.iml
*.sw?

# ── Test / Assets ───────────────────────────────────────────────────────────
**/src/test/
**/src/it/
**/src/integrationTest/
**/src/contractTest/

# ── OS / Others ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
