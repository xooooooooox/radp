--- # sse mode
spring:
  ai:
    mcp:
      server:
        name: ${spring.application.name}
        version: ${spring.application.version}

--- # 若要使用, stdio mode, 启用下面的配置
#spring:
#  main:
#    banner-mode: off
#    web-application-type: none
#  ai:
#    mcp:
#      server:
#        stdio: true
#logging:
#  pattern:
#    console:
