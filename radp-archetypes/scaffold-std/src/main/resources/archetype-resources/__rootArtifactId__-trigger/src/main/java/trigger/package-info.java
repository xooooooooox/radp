#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
/**
 * Trigger layer package.
 * <p>
 * This package contains components that handle external events and triggers that initiate
 * application processes. These include: - HTTP controllers and endpoints - Message queue
 * consumers - Scheduled jobs and tasks - RPC service implementations
 * <p>
 * The trigger layer is responsible for receiving external requests and translating them
 * into calls to the application's domain services.
 */
package ${package}.trigger;
