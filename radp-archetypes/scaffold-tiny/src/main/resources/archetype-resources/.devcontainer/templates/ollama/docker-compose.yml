networks:
  local-network:
    driver: bridge
    name: ${appName}-network
    external: false

services:
  ollama:
    image: ollama/ollama:${symbol_dollar}{OLLAMA_VERSION}
    restart: unless-stopped
    ports:
      - "11434:11434"
