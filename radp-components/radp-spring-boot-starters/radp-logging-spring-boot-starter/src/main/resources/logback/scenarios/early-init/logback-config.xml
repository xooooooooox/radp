<?xml version="1.0" encoding="UTF-8"?>
<!--
早期初始化场景的日志配置
专为在Spring Boot完全初始化之前就需要记录日志的场景设计，例如Testcontainers启动时的日志记录。

特点：
1. 不依赖Spring Boot的日志扩展（如%clr和%wEx转换模式）
2. 使用标准的Logback模式，确保在Spring Boot初始化前就能正确格式化日志
3. 保持基本的日志格式（时间、级别、线程、logger、消息）
-->

<included>
    <include resource="logback/components/early-init/console.xml"/>
    <include resource="logback/components/early-init/file.xml"/>

    <!-- 由于单元测试或集成测试中部分日志无法被Spring Boot控制 -->
    <root level="${logging.level.root:-info}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC"/>
    </root>
</included>